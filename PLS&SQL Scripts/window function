SELECT
booking_id,
passenger_id,
total_amount,

RANK() OVER
(ORDER BY total_amount DESC) AS Rank

FROM Booking;

running total
SELECT

booking_id,
total_amount,

SUM(total_amount)
OVER(ORDER BY booking_id)
AS Running_Total

FROM Booking;
