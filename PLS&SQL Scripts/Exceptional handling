SET SERVEROUTPUT ON;

DECLARE

v_name Passenger.first_name%TYPE;

BEGIN

SELECT first_name
INTO v_name
FROM Passenger
WHERE passenger_id=100;

DBMS_OUTPUT.PUT_LINE(v_name);

EXCEPTION

WHEN NO_DATA_FOUND THEN

DBMS_OUTPUT.PUT_LINE('Passenger not found.');

END;
/
